name: gallagher dev

on:
  push:
  pull_request:
    branches: [ "dev" ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3

      - name: Terraform init
        run: terraform init

      - name: Terraform plan
        run: echo plan

      - name: Terraform apply
        run: echo apply